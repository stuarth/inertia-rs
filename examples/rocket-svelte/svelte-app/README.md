# Svelte Frontend

Vite frontend for the Rocket Inertia example.

```sh
npm install
npm run build
```

The production build writes assets and a Vite manifest to `../public/build`. The Rocket server reads that manifest to choose the script path and asset version.
