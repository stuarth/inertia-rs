[package]
name = "rocket-minimal"
version = "0.1.0"
edition = "2021"
workspace = "../"
publish = false

[dependencies]
inertia_rs = { path = "../..", features = ["rocket"] }
rocket = { version = "0.5.1", features = ["json"] }
serde = { version = "1", features = ["derive"] }
